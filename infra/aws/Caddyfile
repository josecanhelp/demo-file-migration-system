# Fronts control-plane:8080 (the dashboard and its API) with a real Let's
# Encrypt certificate for the domain pointed at this instance. The domain
# comes from the environment so nothing secret is committed; see
# docs/DEPLOYMENT.md for where that value lives.
{$DOMAIN} {
	reverse_proxy control-plane:8080
}
